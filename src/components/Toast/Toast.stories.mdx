import { Meta, Canvas, Story, Props } from "@storybook/addon-docs";
import { ToastNotice } from "./ToastNotice";

<Meta
  title="Catalog/ToastNotice"
  component={ToastNotice}
  args={{ message: "Error", type: "error" }}
/>

export const Template = (args) => {
  const onClose = () => {};
  return <ToastNotice onClose={onClose} {...args} />;
};

# ToastNotice

#### Toast component for showing alerts.

# Example

<Canvas>
  <Story name="ToastNotice">{Template.bind({})}</Story>
</Canvas>

# API Reference

<Props story={"ToastNotice"} />
