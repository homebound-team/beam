import { Meta, Canvas, Story, Props } from "@storybook/addon-docs";
import {Toast} from "./Toast";

<Meta
  title="Catalog/Toast"
  component={Toast}
  args={{ message: "Error", type: "error" }}
/>

export const Template = (args) => {
  return <Toast  {...args} />;
};

# Toast

#### Toast component for showing alerts.

# Example

<Canvas>
  <Story name="Toast">{Template.bind({})}</Story>
</Canvas>

# API Reference

<Props story={"Toast"} />
