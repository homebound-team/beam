---
description: 
globs: *.test.ts,*.test.tsx
alwaysApply: false
---

# Tests

When writing or editing `*.test.ts` / `*.test.tsx`, follow **[AGENTS.md](mdc:AGENTS.md) → Testing**. That section is the single source of truth (structure, selectors, `useTestIds`, interactions, helpers, and lint commands).
